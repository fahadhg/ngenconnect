# Required — powers search embeddings
GEMINI_API_KEY=

# Optional — best available is used for chat (priority order below)
# 1. Claude Sonnet 4.6
ANTHROPIC_API_KEY=
# 2. GPT-4.1
OPENAI_API_KEY=
# 3. DeepSeek V3
DEEPSEEK_API_KEY=
# 4. Falls back to Gemini Flash if only GEMINI_API_KEY is set
